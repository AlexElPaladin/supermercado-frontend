export class Producto {

    constructor(_id: string, nombre: string, precio: string, categoria: string, stock: number) {

    }
    _id: string;
    nombre: string;
    precio: number;
    categoria: string;
    stock: number;
}